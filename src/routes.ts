export const publicRoutes = [
  '/',
  '/new-verification',
];

export const authRoutes = [
  '/login',
  '/register',
  '/error',
  '/reset-password',
  '/new-password',
];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_URL_WHEN_LOGGED_IN = '/settings';
export const DEFAULT_URL_WHEN_NOT_LOGGED_IN = '/login';
